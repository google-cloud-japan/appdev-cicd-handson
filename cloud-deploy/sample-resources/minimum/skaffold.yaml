apiVersion: skaffold/v1
kind: Config
build:
  artifacts:
  - image: app
    context: src
deploy:
  kubectl:
    manifests:
      - deploy/k8s/dev/*
profiles:
 - name: prod
   deploy:
     kubectl:
       manifests:
         - deploy/k8s/prod/*
